import { TakenUsernameDirective } from './taken-username.directive';

describe('TakenUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new TakenUsernameDirective(null);
    expect(directive).toBeTruthy();
  });
});
